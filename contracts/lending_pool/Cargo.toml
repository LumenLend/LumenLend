[package]
name = "lending-pool"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk.workspace = true
ltoken = { path = "../ltoken" }
interest-rate-model = { path = "../interest_rate_model" }
